{
  "name": "cf-cli-workers-builds-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "cf deploy --prebuilt"
  },
  "devDependencies": {
    "cf": "0.10.0"
  },
  "engines": {
    "node": ">=22"
  }
}
